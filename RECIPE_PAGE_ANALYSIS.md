# Анализ страницы рецепта и необходимые изменения

## 📊 Сравнение макета и текущей реализации

### ✅ Что уже реализовано:
1. ✅ Заголовок рецепта
2. ✅ Автор и дата
3. ✅ Главное изображение с play-кнопкой
4. ✅ Описание рецепта
5. ✅ Секция Ingredients (For main dish / For the sauce)
6. ✅ Секция Directions с пронумерованными шагами
7. ✅ Sidebar с кнопками PRINT и SHARE
8. ✅ Nutrition Information (но захардкожено)
9. ✅ Other Recipe секция

### ❌ Что нужно добавить/исправить:

#### 1. Разделить время приготовления
**Проблема:** 
- Сейчас одно поле `COOKING_TIME` используется для PREP TIME и COOK TIME
- В макете показаны разные значения: "PREP TIME 15 Minutes" и "COOK TIME 15 Minutes"

**Решение:**
- Добавить в БД: `PREP_TIME` и `COOK_TIME`
- Обновить модель `Recipe.java`
- Обновить DTO и API
- Обновить frontend для отображения разных значений

#### 2. Добавить поля Nutrition в БД
**Проблема:**
- Сейчас значения захардкожены в `RecipesPage.tsx`:
  ```typescript
  <span>219.8 kcal</span>
  <span>19.7 g</span>
  <span>7.8 g</span>
  <span>22.3 g</span>
  <span>214 mg</span>
  ```

**Решение:**
- Добавить в таблицу `CB_RECIPES`:
  - `CALORIES` (NUMBER(10,2))
  - `TOTAL_FAT` (NUMBER(10,2))
  - `PROTEIN` (NUMBER(10,2))
  - `CARBOHYDRATES` (NUMBER(10,2))
  - `CHOLESTEROL` (NUMBER(10,2))
- Обновить модель, DTO, API
- Обновить frontend для динамического отображения

#### 3. Исправить значение Cholesterol
**Проблема:**
- В макете: **37.4 mg**
- В коде: **214 mg**

**Решение:**
- После добавления полей в БД, значения будут браться из базы
- Нужно будет обновить существующие рецепты с правильными значениями

#### 4. Улучшить отображение типа еды
**Текущее состояние:**
- Показывается первая категория рецепта
- Иконка ForkKnife уже есть

**Нужно проверить:**
- Правильно ли отображается иконка
- Соответствует ли стиль макету

## 📋 План действий

### Этап 1: База данных
1. ✅ Создать SQL миграцию (`migration_add_recipe_fields.sql`)
2. Выполнить миграцию на локальной БД
3. Проверить структуру таблицы

### Этап 2: Backend
1. Обновить модель `Recipe.java`:
   - Добавить поля: `prepTime`, `cookTime`, `calories`, `totalFat`, `protein`, `carbohydrates`, `cholesterol`
   - Обновить геттеры/сеттеры
2. Обновить `RecipeDto.java`:
   - Добавить соответствующие поля
3. Обновить `RecipeMapper.java`:
   - Добавить маппинг новых полей
4. Обновить `RecipeService.java`:
   - Обработка новых полей при создании/обновлении

### Этап 3: Frontend
1. Обновить `recipeApi.ts`:
   - Добавить поля в `RecipeDto` интерфейс
2. Обновить `RecipesPage.tsx`:
   - Использовать `prepTime` и `cookTime` вместо одного `cookingTime`
   - Динамически отображать nutrition данные из API
3. Обновить `CreateRecipePage.tsx`:
   - Добавить поля для ввода nutrition данных
   - Добавить поля для PREP TIME и COOK TIME

### Этап 4: Тестирование
1. Проверить отображение страницы рецепта
2. Проверить создание рецепта с новыми полями
3. Проверить обновление существующих рецептов

## 🔧 SQL Миграция

Файл: `CB_Backend/src/main/resources/migration_add_recipe_fields.sql`

**Важно:** 
- Сделайте backup БД перед выполнением миграции
- Проверьте данные после миграции
- Удаление `COOKING_TIME` выполнять только после проверки

## 📝 Примечания

- В макете показано, что PREP TIME и COOK TIME могут быть разными
- Nutrition данные должны быть опциональными (nullable)
- При создании рецепта через админку тоже нужно добавить эти поля
- Значения по умолчанию можно установить в миграции или в коде
