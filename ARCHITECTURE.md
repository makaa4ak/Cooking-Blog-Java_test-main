# 🏗️ Архитектура приложения Cooking Blog

## 📦 Общая структура проекта

Проект состоит из **3 основных частей**:

```
Cooking blog/
├── frontend/          # Публичный сайт (React + TypeScript)
├── CB_Backend/        # Backend API (Spring Boot + Java)
└── cb_admin/          # Админ-панель (React + JavaScript)
```

---

## 🌐 Frontend (Публичный сайт)

### Технологии:
- **React 19** + **TypeScript**
- **Vite** (сборщик)
- **React Router** (навигация)
- **SCSS Modules** (стили)
- **Swiper.js** (слайдеры)

### Структура:
```
frontend/src/
├── api/              # API клиенты для связи с backend
│   ├── authApi.ts    # Авторизация
│   ├── blogApi.ts    # Блоги
│   ├── recipeApi.ts  # Рецепты
│   ├── categoryApi.ts
│   └── filesApi.ts   # Загрузка файлов
│
├── components/       # Переиспользуемые компоненты
│   ├── Button/
│   ├── Card/
│   ├── PostCard/
│   ├── Header/
│   ├── Footer/
│   ├── ImageUploader/
│   └── RecipesSlider/
│
├── pages/            # Страницы приложения
│   ├── HomePage/     # Главная (слайдер рецептов + категории)
│   ├── BlogPage/     # Список блогов (с поиском и пагинацией)
│   ├── BlogPostPage/ # Отдельный пост блога
│   ├── RecipesPage/  # Список рецептов И детальная страница
│   ├── CreateBlogPage/
│   ├── CreateRecipePage/
│   ├── LoginPage/
│   ├── SignUpPage/
│   └── ProfilePage/
│
├── contexts/         # React Context (глобальное состояние)
│   └── AuthContext.tsx  # Хранит данные текущего пользователя
│
└── styles/           # Глобальные стили
    ├── variables.scss # SCSS переменные (цвета, шрифты)
    ├── reset.scss     # Сброс стилей браузера
    └── style.scss     # Основные стили
```

### Как работает Frontend:

1. **Запуск приложения:**
   - `main.jsx` → рендерит `App.jsx`
   - `App.jsx` → настраивает маршруты (React Router)
   - `AuthContext` → проверяет авторизацию через `/api/auth/me`

2. **Навигация:**
   - Пользователь переходит по ссылке → React Router определяет страницу
   - Страница загружает данные через API (`blogApi.ts`, `recipeApi.ts`)
   - Данные отображаются в компонентах

3. **Авторизация:**
   - При входе: `authApi.login()` → отправляет запрос на backend
   - Backend создает сессию → возвращает данные пользователя
   - `AuthContext` сохраняет пользователя в состоянии
   - `ProtectedRoute` проверяет авторизацию перед показом страницы

4. **Загрузка файлов:**
   - Пользователь выбирает изображение → `ImageUploader` или `PhotoUploader`
   - Компонент отправляет файл на `/api/files/upload`
   - Backend сохраняет файл в `uploads/` → возвращает путь
   - Путь сохраняется в форме и отправляется при создании поста/рецепта

---

## 🔧 Backend (API Server)

### Технологии:
- **Spring Boot** (Java фреймворк)
- **Spring Data JPA** (работа с БД)
- **Hibernate** (ORM - Object-Relational Mapping)
- **Oracle Database** (БД)
- **Gradle** (сборщик)

### Структура:
```
CB_Backend/src/main/java/com/cb/backend/
├── CbBackendApplication.java  # Точка входа
│
├── controller/                # REST API контроллеры
│   ├── AuthController.java      # /api/auth/*
│   ├── BlogController.java       # /api/blogs/*
│   ├── RecipeController.java     # /api/recipes/*
│   ├── CategoryController.java   # /api/categories/*
│   ├── UserController.java       # /api/users/*
│   └── FileController.java       # /api/files/*
│
├── service/                   # Бизнес-логика
│   ├── AuthService.java
│   ├── BlogService.java
│   ├── RecipeService.java
│   └── FileStorageService.java
│
├── repository/                # Доступ к БД (JPA Repository)
│   ├── UserRepository.java
│   ├── BlogRepository.java
│   ├── RecipeRepository.java
│   └── ...
│
├── model/                     # Модели данных (Entity)
│   ├── User.java              # Таблица CB_USERS
│   ├── Blog.java              # Таблица CB_BLOGS
│   ├── Recipe.java            # Таблица CB_RECIPES
│   ├── Category.java          # Таблица CB_CATEGORY
│   ├── Ingredient.java        # Таблица CB_INGREDIENTS
│   └── ...
│
├── dto/                       # Data Transfer Objects (для API)
│   ├── UserDto.java
│   ├── BlogDto.java
│   ├── RecipeDto.java
│   └── ...
│
├── mapper/                    # Конвертация Entity ↔ DTO
│   ├── UserMapper.java
│   ├── BlogMapper.java
│   └── RecipeMapper.java
│
└── GlobalExceptionHandler.java  # Обработка ошибок
```

### Как работает Backend:

1. **Запуск:**
   - `CbBackendApplication.main()` запускает Spring Boot
   - Spring Boot читает `application.properties` → подключается к Oracle БД
   - Hibernate проверяет схему БД (`ddl-auto=update`) → обновляет таблицы при необходимости

2. **Запрос от Frontend:**
   ```
   Frontend → GET /api/recipes/1
              ↓
   RecipeController.getById(1)
              ↓
   RecipeService.findById(1)
              ↓
   RecipeRepository.findById(1) → SQL запрос к БД
              ↓
   RecipeMapper.toDto(recipe) → конвертация в DTO
              ↓
   Возврат JSON → Frontend
   ```

3. **Создание рецепта:**
   ```
   Frontend → POST /api/recipes (JSON с данными)
              ↓
   RecipeController.create(recipeDto)
              ↓
   RecipeService.create(recipeDto)
              ↓
   RecipeMapper.updateEntity() → создает Recipe entity
              ↓
   RecipeRepository.save(recipe) → INSERT в БД
              ↓
   Сохранение ингредиентов (cascade)
              ↓
   RecipeMapper.toDto() → возврат созданного рецепта
   ```

4. **Авторизация:**
   - `AuthController.login()` → проверяет email/password
   - Создает HTTP сессию → сохраняет `userId` в сессии
   - `AuthController.getCurrentUser()` → читает `userId` из сессии

5. **Загрузка файлов:**
   - `FileController.uploadFile()` → получает файл
   - `FileStorageService.storeFile()` → сохраняет в `uploads/{folder}/`
   - Возвращает путь к файлу
   - `FileController.getFile()` → отдает файл по запросу

---

## 🗄️ База данных (Oracle)

### Таблицы:

```
CB_USERS              # Пользователи
├── ID (PK)
├── USERNAME
├── EMAIL
├── PASSWORD_HASH
├── FIRST_NAME
├── LAST_NAME
├── PHOTO_URL
└── ROLE

CB_BLOGS              # Блог-посты
├── ID (PK)
├── TITLE
├── DESCRIPTION
├── TEXT (LONG)
├── PHOTO_URL
├── COOKING_TIME
├── USER_ID (FK → CB_USERS)
├── CREATED_AT
└── UPDATED_AT

CB_RECIPES            # Рецепты
├── ID (PK)
├── TITLE
├── DESCRIPTION
├── TEXT (LONG)
├── PHOTO_URL
├── COOKING_TIME      # Устаревшее (для совместимости)
├── PREP_TIME         # Новое поле
├── COOK_TIME         # Новое поле
├── CALORIES          # Новое поле
├── TOTAL_FAT         # Новое поле
├── PROTEIN           # Новое поле
├── CARBOHYDRATES     # Новое поле
├── CHOLESTEROL       # Новое поле
├── USER_ID (FK → CB_USERS)
├── CREATED_AT
└── UPDATED_AT

CB_CATEGORY           # Категории
├── ID (PK)
├── NAME
├── DESCRIPTION
└── PHOTO_URL

CB_RECIPE_CATEGORIES  # Связь рецептов и категорий (Many-to-Many)
├── RECIPE_ID (FK)
└── CATEGORY_ID (FK)

CB_INGREDIENTS        # Ингредиенты рецептов
├── RECIPE_ID (FK, часть PK)
├── PRODUCT_ID (FK, часть PK)
├── QUANTITY
└── UNIT

CB_PRODUCT            # Продукты
├── ID (PK)
└── NAME

CB_COMMENTS           # Комментарии к рецептам
├── ID (PK)
├── TEXT
├── USER_ID (FK)
├── RECIPE_ID (FK)
├── CREATED_AT
└── UPDATED_AT

CB_RATINGS            # Рейтинги рецептов
├── ID (PK)
├── RATING
├── USER_ID (FK)
└── RECIPE_ID (FK)
```

### Связи:
- **User → Blogs**: One-to-Many (один пользователь → много блогов)
- **User → Recipes**: One-to-Many (один пользователь → много рецептов)
- **Recipe → Categories**: Many-to-Many (много рецептов ↔ много категорий)
- **Recipe → Ingredients**: One-to-Many (один рецепт → много ингредиентов)
- **Ingredient → Product**: Many-to-One (много ингредиентов → один продукт)

---

## 🔐 Админ-панель (cb_admin)

### Технологии:
- **React** + **JavaScript**
- **Vite**
- Те же API что и frontend

### Назначение:
- Управление контентом (CRUD операции)
- Создание/редактирование блогов, рецептов, категорий
- Управление пользователями
- Просмотр всех данных в таблицах

---

## 🔄 Поток данных (Data Flow)

### Пример: Просмотр рецепта

```
1. Пользователь открывает /recipes/5
   ↓
2. RecipesPage.tsx загружается
   ↓
3. useEffect() вызывает getRecipe(5)
   ↓
4. recipeApi.ts → fetch('http://localhost:8080/api/recipes/5')
   ↓
5. RecipeController.getById(5)
   ↓
6. RecipeService.findById(5)
   ↓
7. RecipeRepository.findById(5) → SQL: SELECT * FROM CB_RECIPES WHERE ID = 5
   ↓
8. Hibernate загружает связанные данные (User, Categories, Ingredients)
   ↓
9. RecipeMapper.toDto(recipe) → конвертирует в JSON
   ↓
10. Возврат JSON → Frontend
    ↓
11. RecipesPage отображает данные в компонентах
```

### Пример: Создание рецепта

```
1. Пользователь заполняет форму CreateRecipePage
   ↓
2. Нажимает "Создать рецепт"
   ↓
3. handleSubmit() собирает данные из формы
   ↓
4. createRecipe(recipeDto) → POST /api/recipes
   ↓
5. RecipeController.create(recipeDto)
   ↓
6. RecipeService.create(recipeDto):
   - Проверяет данные
   - Создает Recipe entity
   - Сохраняет рецепт → получает ID
   - Создает ингредиенты с привязкой к рецепту
   - Сохраняет ингредиенты (cascade)
   ↓
7. Возврат созданного рецепта
   ↓
8. Frontend перенаправляет на /recipes
```

---

## 🎨 Стилизация

### SCSS структура:
- **variables.scss**: Цвета (`$white`, `$black`, `$sky-blue`, `$gray`), миксины для типографии
- **reset.scss**: Сброс стилей браузера
- **style.scss**: Глобальные стили, контейнеры

### Компоненты:
- Каждый компонент имеет свой `.module.scss` файл
- Используются миксины из `variables.scss` для единообразия
- Адаптивный дизайн через media queries

---

## 🔒 Безопасность

1. **Авторизация:**
   - HTTP сессии (Spring Session)
   - Проверка авторизации в `ProtectedRoute`
   - Пароли хешируются (BCrypt)

2. **Валидация:**
   - Backend проверяет обязательные поля
   - Frontend показывает ошибки пользователю

3. **CORS:**
   - Настроен в `WebConfig.java` для разрешения запросов с frontend

---

## 📁 Хранение файлов

```
CB_Backend/uploads/
├── avatars/          # Аватары пользователей
├── blog/             # Главные изображения блогов
├── blog-content/     # Изображения внутри текста блогов
├── recipe/           # Главные изображения рецептов
├── recipe-content/   # Изображения внутри текста рецептов
└── category/         # Изображения категорий
```

Файлы сохраняются с UUID именами для избежания конфликтов.

---

## 🚀 Запуск приложения

### Backend:
```bash
cd CB_Backend
./gradlew bootRun
# Запускается на http://localhost:8080
```

### Frontend:
```bash
cd frontend
npm install
npm run dev
# Запускается на http://localhost:5173
```

### Админ-панель:
```bash
cd cb_admin
npm install
npm run dev
# Запускается на http://localhost:5174 (или другой порт)
```

---

## 📝 Основные фичи

1. **Блоги:**
   - Список с поиском и пагинацией
   - Детальная страница поста
   - Создание постов (только авторизованные)
   - Загрузка изображений

2. **Рецепты:**
   - Список с поиском и пагинацией
   - Детальная страница рецепта
   - Ингредиенты с количеством
   - Пошаговые инструкции
   - Информация о питательности
   - Создание рецептов (только авторизованные)

3. **Пользователи:**
   - Регистрация
   - Вход/выход
   - Профиль с аватаром

4. **Категории:**
   - Отображение на главной странице
   - Фильтрация рецептов по категориям

---

## 🔄 Автоматическое обновление БД

Благодаря `spring.jpa.hibernate.ddl-auto=update`:
- При запуске backend Hibernate проверяет схему БД
- Если в модели Java есть новые поля → автоматически добавляются колонки в таблицы
- **Важно:** Только добавление, не удаление или переименование!

---

## 🎯 Итого

**Frontend** (React) → запрашивает данные → **Backend** (Spring Boot) → обращается к **БД** (Oracle) → возвращает данные → **Frontend** отображает

Все работает через REST API, данные в формате JSON, файлы хранятся на сервере.
